# This is an example environment file for the Subastik server.
# Rename this file to `.env` and update the values as needed.

DATABASE_URL="postgresql://postgres:password@localhost:5432/subastik"
JWT_SECRET="un_secreto_muy_seguro_y_largo_para_produccion"
JWT_EXPIRES_IN="7d"